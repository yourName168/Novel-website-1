const loginSelector = document.querySelector('.login');
loginSelector.addEventListener('click', async () => {
    window.location.href = "./login.html";
}
)
const usernameselector = document.querySelector('.username');
const passwordselector = document.querySelector('.password');
console.log(usernameselector.innerHTML);